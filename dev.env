# Add project root to Python path for local development
export PYTHONPATH="${PYTHONPATH}:$(pwd)"

# Optionally, add specific package paths if needed
# export PYTHONPATH="${PYTHONPATH}:$(pwd)/vision_system:$(pwd)/video_streamer"

# Print current PYTHONPATH for verification
echo "PYTHONPATH is now: $PYTHONPATH" 