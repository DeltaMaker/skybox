# This file describes the Python virtualenv package requirements for
# the Skybox software. These package requirements are
# typically installed via the command:
#   pip install -r netinfo-requirements.txt
flask
